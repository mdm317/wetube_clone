extends templates/layout

block content
    h1 Add Movie
    .form-container
        form(action="/create", method="post")
            input(type="text", pattern=".{3,}" , name="title", placeholder="title", required=true, title="3글자 이상")
            input(type="number", name="year", placeholder="Year", required=true)
            input(type="text", name="rating", placeholder="Rating", required=true)
            textarea(name="synopsis", placeholder="synopsis", required=true)
            input(type="text", name="genres", placeholder="genres", required=true)
            input(type="submit", value="Submit")